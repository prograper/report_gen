下面是受试者基本信息 (CSV)：

{{ table }}

请返回 subject_id, age, weight 三个字段，
使用函数 "extract" 以 JSON 形式返回。

你是一名药代动力学数据分析师。请分析我提供的表格，并提取以下两个指标：
表格信息：
{{ table }}

1.  **Dosage (给药剂量)**: 实验中使用的给药剂量是多少？
2.  **T_max (达峰时间)**: “尿钙排泄率 (ng/kg/mL)”达到峰值（最大值）时，所对应的时间点“时间点 (h)”是多少？

请返回 Dosage, T_max 两个字段，使用函数 "extract" 以 JSON 形式返回。